````py
Map-generator/
│── main.py               # Archivo principal que ejecuta el programa
│── generator.py          # Genera el mapa usando ruido Perlin
│── visualizer.py         # Se encarga de mostrar y guardar la imagen
│── utils.py              # Funciones auxiliares (normalización, colores, etc.)
│── config.json           # Configuración del tamaño, escala, octavas, etc.
│── readme.md
│── requirements.md
└── maps/                 # Carpeta donde se guardan los mapas generados
```